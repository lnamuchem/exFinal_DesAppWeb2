import { EntidadIngrediente } from './entidad-ingrediente';

describe('EntidadIngrediente', () => {
  it('should create an instance', () => {
    expect(new EntidadIngrediente()).toBeTruthy();
  });
});
